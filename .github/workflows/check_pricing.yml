name: Price Calculator Checker

on:
  push:
    branches: [main, "fix/**"]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🐍 Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: 🧪 Run tests
        run: python -m unittest discover tests -v

      - name: ✅ Success message
        if: success()
        run: |
          echo "🎉 All tests passed!"
          echo "Great job fixing the price calculator!"

      - name: ❌ Failure message
        if: failure()
        run: |
          echo "😢 Some tests failed!"
          echo "Check the error messages above for hints."
